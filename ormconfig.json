{
   "type": "mysql",
   "host": "localhost",
   "port": 3306,
   "username": "root",
   "password": "mario.balukja",
   "database": "task_project",
   "synchronize": true,
   "logging": false,
   "entities": [
      "dist/entity/**/*.{js,ts}"
   ],
   "migrations": [
      "dist/migration/**/*.{js,ts}"
   ],
   "subscribers": [
      "dist/subscriber/**/*.{js,ts}"
   ],
   "cli": {
      "entitiesDir": "src/entity",
      "migrationsDir": "src/migration",
      "subscribersDir": "src/subscriber"
   }
}
